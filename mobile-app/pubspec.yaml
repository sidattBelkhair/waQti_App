name: waqti_app
description: WaQti - Gestion des files d'attente

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  go_router: ^14.0.0
  dio: ^5.7.0
  socket_io_client: ^2.0.3+1
 
  shared_preferences: ^2.2.3
  flutter_rating_bar: ^4.0.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true